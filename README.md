Simple app for making ToDo lists

### Installation and running:

```
virtualenv env
source env/bin/activate
pip install -r requirements.txt
python manage.py syncdb
python manage.py runserver
```

Login page is `/todo/login`
